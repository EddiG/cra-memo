{
  "name": "cra-memo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "lodash": "^4.17.4",
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "react-redux": "^5.0.5",
    "recompose": "^0.23.4",
    "redux": "^3.6.0",
    "redux-saga": "^0.15.3",
    "styled-components": "^2.0.0"
  },
  "devDependencies": {
    "react-scripts": "1.0.7",
    "react-test-renderer": "^15.5.4"
  },
  "scripts": {
    "start": "NODE_PATH=src react-scripts start",
    "build": "NODE_PATH=src react-scripts build",
    "test": "NODE_PATH=src react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "surge -p ./build -d memo.surge.sh"
  }
}
